import pandas as pd

# function to optimize data types


def optimize_data_types(df: pd.DataFrame) -> pd.DataFrame:
    """
    Optimize data types of a DataFrame to reduce memory usage.

    Args:
        df (pd.DataFrame): input Dataframe to optimize.

    Returns:
        pd.DataFrame: outputs a DataFrame with optimized data types.
    """


# Work in progress: Implement data type optimization logic
